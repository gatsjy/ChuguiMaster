# 변경 이력

이 프로젝트는 [Semantic Versioning](https://semver.org/lang/ko/)을 따릅니다.

---

## [2.1.0] — 2026-08-21

실수를 되돌릴 수 있게 만드는 릴리스입니다.

v2.0은 **크래시**에는 강했습니다. 원자적 저장 덕분에 전원이 나가도 파일이 깨지지
않았고 최대 손실은 600ms였습니다. 하지만 **사람의 실수**에는 무방비였습니다.
`전체 비우기` 를 잘못 누르면 자동 저장이 그 결과를 성실하게 저장할 뿐이었고,
`.bak` 은 직전 저장본이라 되돌릴 지점이 되지 못했습니다.
진짜 위험은 전원 차단이 아니라 오클릭이었습니다.

### 🔴 파서 버그 (실측)

사용자가 엑셀에서 복사한 225행 명단으로 검증하다 발견했습니다.

**소속 열 유실** — 텍스트 파서가 `belong` 을 채우지 않아,
`이름<TAB>금액<TAB>소속` 을 붙여넣어도 소속이 `raw` 에만 남았습니다.
엑셀로 내보내면 소속 열이 통째로 비었습니다.

이제 탭 또는 2칸 이상 공백으로 구분된 줄을 **표로 인식**합니다.
이름은 이름 열에서만 고르고, 금액은 금액 열에서만 읽고, 나머지는 소속이 됩니다.
소속에 숫자가 섞여 있어도(`세교4리`) 금액이 흔들리지 않습니다.
한 칸 공백 자유 서식(`홍길동 10만원 친척`)은 종전대로 동작합니다.

**관계 오분류** — 부분 문자열이 단어 경계를 가로질러 매치했습니다.

```
'여명교회사랑부' → 직장/기관 (오답)
  '교회' @2 (종교)  vs  '회사' @3 (직장)  ← 여명교[회사]랑부
  길이가 같아(둘 다 2자) tie-break 로 직장이 이김
```

겹치는 매치는 먼저 시작한 쪽만 남기도록 고쳤습니다. 한국어 합성어는 의미의
머리가 앞에 오므로 뒤에 걸린 쪽이 우연입니다. 겹치지 않는 매치끼리는
종전대로 최장 일치가 이깁니다(`대학병원` 은 직장 그대로).

### ↩️ 되돌리기

**파괴 연산 직후 되돌리기 버튼** — `전체 비우기` · 파싱 덮어쓰기 · 파일 병합 ·
샘플 불러오기 직후 토스트에 `되돌리기` 버튼이 9초간 뜹니다. 한 번 누르면 복구됩니다.

**시점 스냅샷** — 파괴 연산 **직전**과 5분마다 자동으로 시점을 남깁니다.
링 버퍼로 최대 20개 유지. `이전 시점 복구` 버튼에서 시점을 골라 되돌립니다.
목록에 시각·사유·건수·총액이 함께 나옵니다.
복구 직전에도 시점을 남기므로 복구 자체를 다시 취소할 수 있습니다.

`전체 비우기` 는 세션 파일과 `.bak` 을 지우지만 **스냅샷은 남습니다.**
v2.0에서 이 조합은 되돌릴 방법이 전혀 없었습니다.

**셀 편집 Undo/Redo** — `Ctrl+Z` / `Ctrl+Y`. 금액·이름·관계·참석·식권·발송 체크가
모두 대상입니다. 참석을 불참으로 바꾸면 식권이 0이 되는데, 되돌리면 식권도 함께
돌아옵니다. 목록이 통째로 바뀌면 행 번호가 무의미해지므로 이력을 비웁니다.

**비정상 종료 감지** — 기동 시 표식 파일을 만들고 정상 종료 시 지웁니다.
다음 기동에 표식이 남아 있으면 알립니다.

> 지난번(07월 29일 21:14 시작)에 프로그램이 정상적으로 종료되지 않았습니다.
> 마지막 작업 225건을 복구했습니다.

사용자가 자기 작업이 살아 있는지 모른 채 불안해하지 않도록 하는 것이 목적입니다.

### ✍️ 입력

**실시간 미리보기** — 입력창 아래에 300ms 디바운스로 해석 결과를 보여 줍니다.

```
미리보기 · 225건 · 28,470,000원 · 확인 필요 2건
```

파싱을 누르기 전에 결과를 알 수 있어, 틀린 줄을 찾으려고 표까지 갔다 오는
왕복이 사라집니다.

**추가 모드** — `기존 목록에 추가` (`Ctrl+Shift+Enter`).
v2.0까지 파싱은 곧 전체 교체였습니다. 명단을 나눠서 적는 사람은 새 줄 몇 개를
넣으려고 전체를 다시 붙여넣어야 했습니다. 이름과 금액이 모두 같으면 중복으로
보고 건너뛰고, 이름만 같으면 `중복 의심` 으로 표시합니다.

**서식 없는 붙여넣기** — 카톡에서 복사할 때 딸려오던 글꼴·색이 사라집니다.

### 🧪 테스트

352 → **444개**. 새로 추가한 영역:

| 파일 | 개수 | 범위 |
|---|---:|---|
| `test_columns.py` | 28 | 탭/공백 열 인식 · 관계 겹침 제거 |
| `test_snapshots.py` | 18 | 링 버퍼 · 목록 · 되돌리기 |
| `test_undo.py` | 16 | Undo/Redo · 스택 위생 |
| `test_crash_recovery.py` | 13 | 표식 · 복구 다이얼로그 |

---

## [2.0.2] — 2026-08-07

세션 복구의 데이터 관용성과 UI 저장 효율을 개선했습니다.

### 수정

- 실행 화면·패키지 메타데이터·문서의 버전을 `2.0.2`로 통일했습니다.
- 구버전이나 수동 편집 세션에서 문자열 `"false"`가 발송 완료로 복구되던 문제를 수정했습니다.
- 손상된 세션의 음수 금액과 음수 식권 수를 0으로 보정합니다.
- 표 편집 한 번에 요약 계산과 자동 저장이 두 번 예약되던 중복 연결을 제거했습니다.
- 테마 변경과 감사 메시지 표시 갱신은 데이터 변경이 아니므로 세션을 다시 저장하지 않습니다.
- 식대 변경 시 표 구조가 바뀐 것처럼 알리던 불필요한 `layoutChanged` 신호를 제거했습니다.

### 테스트

- 문자열 불리언, 음수 세션 데이터, 버전 일치, 불필요한 자동 저장 방지 회귀 테스트를 추가했습니다.

---

## [2.0.1] — 2026-07-30

실제 하객 명단 225건으로 검증하다가 발견한 정산 오류를 수정했습니다.

### 🔴 이름에 들어 있는 `대인` / `소인` 이 식권 수로 오인되던 문제

하객 성명에 `대인` 두 글자가 들어 있었습니다. 그것이 식권 표기로 매치되어
바로 뒤의 금액을 식권 수로 읽었고, 금액 파서는 그 부분을 지워버렸습니다.

```
"홍대인    50,000    OO기업 충청사업소"
   → '대인' + 공백 + '50' 을 식권 표기로 매치
   → 대인 식권 50장 (실제 1명)
   → 금액에서 "대인 50" 이 제거되어 ",000" 만 남아 0원
```

225건 명단 전체에 미친 영향:

| 항목 | 정답 | 수정 전 | 오차 |
|---|---:|---:|---:|
| 총 수령 축의금 | 28,470,000 | 28,420,000 | −50,000 |
| 대인 식권 | 248장 | 297장 | +49장 |
| 총 식대 | 13,640,000 | 16,335,000 | +2,695,000 |
| **최종 순 정산금** | **14,830,000** | **12,085,000** | **−2,745,000** |

이름에 `대인` `소인` `성인` `식대` `아이` 가 포함된 하객이 모두 같은 영향을 받았습니다.

**수정** — 식권 표기 파싱을 `parsing/tickets.py` 한 곳으로 모으고 경계를 세 겹으로 막았습니다.
금액 파서와 텍스트 파서가 각자 비슷한 정규식을 들고 있던 것이 문제를 키웠습니다.

1. **앞 경계** — 한글 바로 뒤에 붙은 키워드는 사람 이름의 일부로 봅니다.
   단 `만` `천` `억` `원` `장` `명` 뒤라면 허용해 `10만원식권2` 붙여쓰기를 계속 지원합니다.
2. **자릿수** — 식권 수는 1~2자리이며 더 긴 숫자의 앞부분이어서는 안 됩니다.
   이 조건만으로도 `50,000` 을 식권 수로 읽는 일이 사라집니다.
3. **구분자** — 키워드와 숫자 사이의 탭·줄바꿈은 다른 열로 봅니다.
   탭 구분 명단을 붙여넣는 경우가 흔합니다.

### ✨ 단체 명의 하객

`OO교회청년부` 처럼 단체 명의로 낸 경우 이름을 인식하지 못해 `하객83` 으로 표시됐습니다.
이제 그 이름을 그대로 쓰되, 사람 이름인지 확신할 수 없으므로 `확인 필요` 표시는 유지합니다.

### 🧪 테스트

- `tests/test_tickets.py` 추가 — 이번 사고를 회귀 테스트로 고정(32개).
  이름 속 식권 단어, 문서화된 식권 표기 8종, 탭 구분 명단, 정산 정확도, 단체 명의.
- 전체 320 → **352개**, 전부 통과.

---

## [2.0.0] — 2026-07-29

v1의 **정산 신뢰성 문제**를 해결하기 위한 전면 재작성입니다.
v1은 UI 완성도는 높았지만 내장 샘플 데이터에서조차 금액이 6배로 계산됐고,
소스를 clone한 사용자는 첫 실행에서 크래시를 봤습니다.
이 릴리스의 목표는 기능 추가가 아니라 **화면에 뜬 숫자를 믿을 수 있게 만드는 것**입니다.

### 🔴 정확도 — 잘못 계산되던 것들

#### 금액 파서 전면 재작성 (`parsing/amount.py`)

v1은 줄 전체에서 `re.findall(r'\d+')` 로 숫자를 긁어 **이어붙였습니다**.

| 입력 | v1 | v2 |
|---|---:|---:|
| `1 홍길동 200000 친척모임` | 1,200,000 | **200,000** |
| `5 최동료 100000 A보건지소` | 5,100,000 | **100,000** |
| `10 박대현 1000000 고모` | 101,000,000 | **1,000,000** |
| `26 진부부,신부부 100000` | 26,100,000 | **100,000** |
| `정삼촌 300000 삼촌 식권2 소인1` | 30,000,021 | **300,000** |
| 내장 샘플 27건 총액 | 수천만원대 | **6,600,000** |

접근을 뒤집었습니다. "숫자를 모으기"가 아니라 **"금액이 아닌 것을 먼저 제거하기"** 입니다.

- 줄번호 · 식권 수 · 날짜 · 시각 · 전화번호 · 계좌번호를 선제 제거
- `숫자 + 단위` 구조로 후보를 추출 (억 · 천만 · 백만 · 십만 · 만 · 천)
- `10만 5천` = 105,000, `3억 2천만` = 320,000,000 지원
- **단위 없는 1,000 미만 숫자는 금액으로 인정하지 않음** — v1은 `500` 을 500만원으로 승격시켰습니다
- 후보가 2개 이상이면 최댓값을 쓰되 `확인 필요` 로 표시

#### 정산식을 README 명세대로 수정 (`services/settlement.py`)

v1: `total_meal_fee = total_guests * adult_cost` (하객 **명** 수 × 대인 단가)

| | v1 | v2 |
|---|---|---|
| 식대 기준 | 하객 명 수 | **발급 식권 수** |
| `식권2` 파싱 결과 | 사용 안 함 | **반영** |
| 소인 단가 | 계산에 미반영 (장식) | **반영** |
| 불참(송금) 하객 | 식대 차감됨 | **제외** |

```
총 식대     = 대인 식권 수 × 대인 단가 + 소인 식권 수 × 소인 단가
최종 순정산 = 총 축의금 − 총 식대
```

#### 이름 추출 재작성 (`parsing/names.py`)

v1은 `re.sub(r'[\d만,원식권불참송금현금계좌이체]', '', token)` 으로 **글자 단위** 삭제를 했습니다.
한국 이름에 흔한 '이' '체' '권' '원' '식' '금' '현' '계' '좌' 가 지워졌습니다.

| 입력 | v1 | v2 |
|---|---|---|
| `이체리 100000 친구` | `하객7` | **이체리** |
| `권원식 100000 지인` | `하객7` | **권원식** |
| `박이모 1000000 B이모` | `하객10` | **박이모** |
| `김성도(조성도) 50000` | 김성도(조성도) | **김성도** + 별칭 `조성도` |

- "지우기"가 아니라 "고르기": 한글 2~5자 토큰 중 조직/상태어가 아닌 첫 토큰
- 조직 접미사는 2글자 이상만 사용 (1글자 접미사는 `박서원` `한계장` 같은 실명을 잡아먹음)
- 괄호 별칭 분리, `,` `·` `/` `&` 복수 이름 분리
- 못 찾으면 추측하지 않고 `이름 미인식` 경고

#### 관계 분류를 최장 일치로 변경 (`parsing/relations.py`)

v1은 dict 선언 순서상 처음 걸린 키워드를 채택해 결과가 뒤집혔습니다.
v2는 **가장 긴 키워드가 이기고**, 동점이면 친척 > 직장 > 종교 > 학교 > 기타 순입니다.

### 🔴 안정성 — 죽던 것들

| 증상 | 원인 | 수정 |
|---|---|---|
| **clone 후 첫 실행 크래시** | 저장소에 커밋된 `autosave_session.json` 의 부분 레코드 → `KeyError: 'relation'` | `Guest.from_dict()` 가 어떤 dict도 흡수 + 데이터 파일 저장소에서 제거 |
| **CSV 드롭 시 크래시** | 파일 다이얼로그·드래그&드롭은 `.csv` 를 받는데 `pd.read_excel` 만 호출 | 표준 `csv` 모듈로 실제 지원 (utf-8-sig / cp949 / euc-kr 자동 판별) |
| **템플릿 편집 후 표 전체 사망** | 사용자 문구의 `{` 하나로 `str.format` 이 `KeyError` → 렌더링 루프에서 터짐 → JSON에 영구 저장되어 재실행해도 계속 죽음 | 정규식 치환으로 전환. 모르는 자리표시자는 원문 유지, 예외 없음 |
| **표 편집이 조용히 사라짐** | `QTableWidgetItem` 은 기본 편집 가능인데 `guest_data` 에 반영되지 않아 다음 렌더링에 원복 | Model/View 전환. `setData` 로 즉시 반영 + 자동 저장 |
| **두 번째 토스트부터 즉시 사라짐** | `fade_out()` 마다 `finished → hide` 연결이 누적되고 그 연결이 fade-**in** 에도 걸림 | 생성자에서 1회만 연결, 방향 플래그로 분기 |
| **관계 변경 시 크래시 위험** | 콤보 시그널 핸들러가 `render_table()` 호출 → `setRowCount(0)` 이 시그널 발신 중인 콤보를 파괴 | 셀 위젯을 델리게이트로 대체, 재진입 구조 자체를 제거 |
| **명단이 역순으로 표시** | Qt의 `setSortingEnabled(True)` 는 0번 열 **내림차순**이 기본 | 입력 순서(오름차순)로 명시 지정 |
| **저장 실패를 사용자가 모름** | `except Exception: pass` | 로그 기록 + 상태바 안내 |
| **세션 파일 손상 시 복구 불가** | 대상 파일을 직접 덮어씀 | 임시 파일 + `os.replace` + `.bak` 자동 복구 |

### ✨ 기능

- **현금 + 계좌이체 병합 구현** (`services/merge.py`)
  v1에는 이 기능이 **존재하지 않았습니다**. 텍스트 파싱과 엑셀 로딩 모두
  `self.guest_data = parse_...()` 로 덮어써서, 현금 명단을 취합한 뒤 엑셀을 드롭하면
  현금 내역이 전부 사라졌습니다. README가 내세운 핵심 기능 2번이 코드에 없었습니다.
  같은 이름은 지우지 않고 `중복 의심` 으로 표시합니다 — 동명이인인지 중복 기입인지는 사람만 압니다.

- **`⚠ 확인 필요` 시스템**
  파서가 확신하지 못하면 조용히 추측하지 않고 표시합니다.
  KPI 카드에 건수가 뜨고, `확인 필요만` 필터로 해당 행만 볼 수 있습니다.
  "틀린 값을 자신 있게 보여주는 것"이 v1의 근본 문제였습니다.

- **은행 엑셀 헤더 자동 탐색**
  토스·카카오뱅크·시중은행 내역처럼 상단에 계좌·조회기간 안내가 붙은 파일도 읽습니다.
  `보낸분` `입금액` `기재내용` `적요` 컬럼명 인식, 하단 합계/소계 행 자동 제외.

- 표 직접 편집(이름·금액·관계·참석·식권), 전 열 정렬(금액은 숫자 기준)
- 미발송 필터, 관계 필터, 실시간 검색(이름·소속·비고·원문·별칭)
- 엑셀 내보내기에 서식 추가(헤더 고정 · 자동 필터 · 확인 필요 행 강조)와 `정산 요약` 시트
- 단축키: `Ctrl+Enter` 파싱 · `Ctrl+S` 내보내기 · `Ctrl+F` 검색
- 창 크기·테마 상태 저장

### ✉️ 감사 메시지

- 문구 전면 리라이팅 — 길이 30~40% 축소, 상투어 제거
  (`자리를 빛내주셔서`, `가슴 깊이 새기며`, `예쁘고 행복하게 살겠습니다`, `맛있는 거 쏠게` 등)
- **`{name}야` 호격조사 제거** — 받침 없는 이름에서 `지훈야` 가 되던 문제
- **복수 이름 `분` 접미사 제거** — `김가족 & 김친지 분님,` 이 되던 문제
- 자리표시자 확장: `{name}` `{relation}` `{amount}` `{belong}`
- **복사 ≠ 발송** — v1은 복사 버튼을 누르면 자동으로 발송완료 처리해서,
  문구를 확인만 하려고 눌러도 발송 처리되어 추적이 무의미해졌습니다.
  v2에서 발송 체크는 사용자가 직접 합니다.

### 🎨 UI / UX

#### 디자인 시스템 도입 (`ui/theme.py`)

색 · 간격 · 타이포그래피를 **토큰**으로 정의하고 스타일시트를 거기서 생성합니다.
v1은 40줄짜리 스타일시트 리터럴 두 벌과 별도의 배지 색 문자열 딕셔너리를 들고 있었고,
그 배지 색이 다크 전용이라 라이트 모드에서 대비가 무너졌습니다.

- 8pt 그리드 기반 간격 스케일, 6단계 타이포그래피 스케일
- 최소 클릭 타깃 28px, 표 행 높이 40px
- 포커스 링 · hover · pressed · disabled 상태 전부 정의

#### 접근성 (WCAG 2.1 AA)

모든 텍스트 × 배경 조합이 본문 4.5:1, UI 요소 3:1을 **두 테마 모두**에서 만족합니다.
자동 검증 과정에서 실제로 두 건이 적발되어 수정됐습니다.

| 항목 | 이전 | 이후 |
|---|---:|---:|
| success 버튼 흰 글씨 (`#059669`) | 3.77:1 ❌ | 5.48:1 ✅ (`#047857`) |
| 라이트 테마 순정산 값 | 3.58:1 ❌ | 5.21:1 ✅ |

- 모든 상호작용 컨트롤에 `accessibleName` + 툴팁
- KPI 카드가 `accessibleDescription` 으로 현재 값 노출
- 편집 가능한 열이 그 사실을 툴팁으로 안내

#### 레이아웃

- **KPI 값 자동 맞춤** — 카드 폭에 맞춰 글자 크기가 22 → 20 → 18 → 16 → 13px로 단계 축소.
  축의금 총액은 자릿수 편차가 커서(50만 ~ 3억) 고정 크기로는 큰 금액이 잘립니다.
  짧은 값에서는 큰 글씨를 유지해 위계를 지킵니다.
- **KPI 행 과밀 해소** — 초안은 식대 입력을 포함해 카드 5장을 한 줄에 넣어
  카드 폭이 179px까지 좁아졌고 `100,000원` 조차 기본 크기로 표시되지 못했습니다.
  입력은 지표가 아니므로 별도 설정 줄로 분리해 카드를 273px로 넓혔습니다.
- **최소 창 크기 보장** — 레이아웃 실제 요구치(1,067px)보다 큰 1,160px을 최소로 잡아
  1280×800 노트북에서도 가로 스크롤이 생기지 않습니다.
- 보조 설명은 넘칠 때 `…` 로 줄이고 툴팁에 전문을 담습니다.
- 스플리터로 패널을 0px까지 접을 수 없습니다.

#### 상태 표현

- **빈 상태 안내** — 빈 격자 대신 다음에 할 일을 보여줍니다.
  데이터가 없을 때와 필터 결과가 0건일 때 **다른 문구**를 씁니다.
- **확인 필요 배너** — 문제가 있을 때만 나타나고, 고치면 사라집니다.
  0건일 때 회색 카드를 계속 띄우는 건 노이즈일 뿐입니다.
- **확인 필요 행 강조** — 경고를 한 열에만 적어두면 가로로 넓은 표에서 눈에 띄지 않습니다.
  행 전체를 은은하게 물들여 스크롤 중에도 즉시 보이게 했고, 색은 테마를 따라갑니다.
- **숫자에 근거 표시** — 총 식대 아래 `대인 30장 · 소인 1장`,
  총 축의금 아래 `현금 … · 이체 …` 처럼 계산 근거를 붙여 검산할 수 있게 했습니다.
- **상태바 요약** — 건수 · 평균 금액 · 감사 인사 발송 진행률을 항상 표시합니다.
- 기본 정렬을 입력 순서로 고정했습니다(Qt 기본값은 0번 열 내림차순이라 명단이 역순으로 보였습니다).

#### UX 자동 검증 (`tests/test_ux.py`, 109개)

디자인은 코드 리뷰로 지키기 어렵습니다. 측정 가능한 것은 테스트로 고정했습니다.

- 대비비 — 모든 색 조합 × 2테마, WCAG 상대 휘도 직접 계산(계산기 자체도 검증)
- 접근성 — 레이블 누락, 단축키 등록 및 동작, 툴팁 존재
- 레이아웃 — 최소 크기 적합성, 값 잘림, 토스트 경계, 패널 접힘 방지
- 상태 — 빈 상태 전환, 배너 표시/소멸, 행 강조, 복사 시 발송 미표시
- 토큰 — 스케일 단조성, 최소 본문 크기, 스타일시트 커버리지

### 🏗️ 구조

- **939줄 God object 해체** — `main_view.py` 에 UI 조립 · 비즈니스 로직 · 파일 영속화 ·
  스타일 상수 · 샘플 데이터가 전부 들어 있었습니다. 22개 모듈로 계층 분리했습니다.
- **`parsing` / `services` / `storage` 는 Qt에 의존하지 않습니다** — 핵심 로직 전체가 GUI 없이 테스트됩니다.
- **`@dataclass Guest` 도입** — v1은 raw dict라 파싱 경로마다 키 스키마가 달랐습니다.
- **테스트 0개 → 320개**(UX·접근성 자동 검증 109개 포함), ruff 린트 도입,
  `pyproject.toml` · `requirements.txt` 추가
- `sys.path` 해킹 제거, 정식 패키지 구조(`src/chugui/`)로 전환
- 파일 로깅 도입 (`%LOCALAPPDATA%\ChuguiMaster\logs\`, 1MB × 3세대 회전)
- 테마를 팔레트 기반으로 재구성 — v1의 관계 배지 색은 다크 전용이라 라이트 모드에서 대비가 무너졌습니다

### ⚡ 성능

- **표 렌더링** — v1은 검색창 키 입력 한 번마다 표를 통째로 재구축했습니다.
  행마다 위젯 5개를 새로 만들고 긴 스타일시트를 매번 파싱했으니 300명이면 키 입력 1회에 위젯 1,500개입니다.
  Model/View + 델리게이트로 전환해 위젯 생성이 0개가 됐습니다.
- **자동 저장** — v1은 키 입력 한 번마다 세션 JSON 전체를 디스크에 썼습니다. 600ms 디바운스를 적용했습니다.
- **pandas 제거** — 쓰던 기능은 `read_excel` / `to_excel` 두 개뿐인데
  실행 파일이 두 배가 되고 콜드 스타트가 몇 초씩 늘어났습니다. openpyxl 직결로 전환했습니다.
- **빌드 기본을 `--onedir` 로** — `--onefile` 은 실행할 때마다 전체를 임시 폴더에 압축 해제합니다.
  단일 파일이 필요하면 `python build.py --onefile`.

### 🔐 보안 / 개인정보

- **저장소에 커밋돼 있던 사용자 데이터 파일 제거** — `autosave_session.json`,
  `config_settings.json` 은 하객 실명과 금액이 담기는 파일인데 `.gitignore` 에 없었습니다.
- **소스에 하드코딩된 실명 제거** — `parse_excel` 에 특정 인명이 스킵 조건으로 남아 있었습니다
  (커밋 `f908e84 security: Complete 100% anonymization` 에서 누락된 부분).
  개인정보 노출이자, 동명이인이 첫 행에 오면 조용히 누락시키는 로직 폭탄이었습니다.
- **설정/세션을 OS 표준 사용자 데이터 폴더로 이전** — v1은 상대 경로라
  `--onefile` exe의 실행 위치에 따라 설정이 흩어졌고, `Program Files` 에 두면 쓰기가 실패했습니다.
  v1 파일은 최초 실행 시 자동으로 이전됩니다.
- 네트워크 통신 없음. 하객 명단은 이 PC를 벗어나지 않습니다.

### 📄 문서

- **`FLOW.md` 신규** — 동작 flow 상세 문서. Mermaid 다이어그램 8개(기동 · 텍스트 파싱 ·
  스프레드시트 파싱 · 병합 · 정산 · 표 렌더링 · 메시지 · 저장)와 v1 대조표.
- **`README.md` 전면 개정** — v2 기능 · 설치 · 구조 · 데이터 보관 위치.
- **`CHANGELOG.md` 신규** — 이 문서.

### ⚠️ 호환성

- **`.xls` 지원 중단** — v1도 `xlrd` 가 requirements에 없어 실제로는 동작하지 않았습니다.
  엑셀에서 `.xlsx` 로 저장 후 사용하라는 안내 메시지를 표시합니다.
- v1 세션/설정/템플릿 파일은 자동 마이그레이션되므로 별도 조치가 필요 없습니다.
- 빌드 산출물 이름이 `ChuguiMaster-Single.exe` → `ChuguiMaster.exe` 로 바뀌었습니다
  (v1은 README에 적힌 이름과 실제 산출물 이름이 달랐습니다).

---

## [1.x] — 이전

초기 버전. 스마트 파서 · 감사 메시지 생성기 · PySide6 GUI · 다크 모드 ·
관계 배지 · 드래그&드롭 · 실시간 검색 · 세션 자동 저장.
